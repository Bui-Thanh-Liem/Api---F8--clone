export enum HttpStatusCode {
  ERROR = 500,
  NOT_FOUND = 404,
  SUCCESS = 200,
}

export enum HttpMessage {
  ERROR = 'Server internal error',
  NOT_FOUND = 'Not Found',
  SUCCESS = 'Successfully',
}
